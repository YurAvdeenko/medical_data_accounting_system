name: home
description: A new Flutter package project.
version: 0.0.1

environment:
  sdk: '>=2.19.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  equatable: ^2.0.0
  flutter:
    sdk: flutter

  core:
    path: ../../core
  data:
    path: ../../data
  domain:
    path: ../../domain
  core_ui:
    path: ../../core_ui
  di:
    path: ../../di

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  connectivity_plus: ^2.3.6+1

flutter:

